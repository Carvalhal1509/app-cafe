@*@model List<Usuarios>*@
@{
    ViewData["Title"] = "Listagem de Eventos";
}

    <div class="d-grid gap-2 d-md-flex justify-content-md-start mt-2 ml-3">
        <a class="btn btn-warning btn-lg ms-2" role="button" asp-controller="" asp-action="">Adicionar Evento</a>
        <a class="btn btn-light btn-lg ml-2" role="button" asp-controller="Home" asp-action="Index">Voltar</a>
    </div>
    <br />

    @if (TempData["MensagemSucesso"] != null)
    {<div class="alert alert-success" role="alert">
            <button type="button" class="btn btn-danger btn-sm close-alert" arial-label="Close">X</button>
            @TempData["MensagemSucesso"]
        </div>
    }

    @if (TempData["MensagemErro"] != null)
    {<div class="alert alert-success" role="alert">
            <button type="button" class="btn btn-danger btn-sm close-alert" arial-label="Close">X</button>
            @TempData["MensagemErro"]
        </div>
    }

    <div class="text-center">
        <h1 class="display-4">Lista de Eventos </h1>
        <br>
        <table class="table container">
            <thead>
                <tr>
                    <th scope="col" style="display:none">Id</th>
                    <th scope="col">Nome do Evento</th>
                    <th scope="col">Data</th>
                    <th scope="col">Ação</th>
                </tr>
            </thead>
            <tbody>
                @*@if (Model != null && Model.Any())
                    {
                        foreach (Usuarios contato in Model)
                        {*@
                <tr>
                    <th scope="row" style="display:none">@*@contato.Id*@</th>
                    <td>@*@contato.Nome*@</td>
                    <td>@*@contato.Email*@</td>
                    <!--<td>-->@*@contato.Celular*@
                    <!--</td>
                    <td>-->
                    @*@contato.Aniversario.ToString("dd/MM/yyyy")*@
                    <!--</td>
                    <td>-->
                    @*@(contato.Perfil == app_cadastro.Enums.PerfilEnum.Admin ? "Administrador" : "Padrão")*@<!--</td>-->

                    <td>
                        <div class="btn-group" role="group">
                            <a role="button" class="btn btn-warning btn-lg" asp-route-id="" asp-controller="" asp-action="">Visualizar</a>
                            <a role="button" class="btn btn-light btn-lg" asp-route-id="" asp-controller="" asp-action="">Editar</a>
                            <a role="button" class="btn btn-danger" asp-route-id="" asp-controller="" asp-action="">Apagar</a>
                        </div>
                    </td>

                </tr>
                @*}
                    }*@


            </tbody>
        </table>
    </div>
